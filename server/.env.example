# Each teammate: Neon → your branch → Connect → Prisma → copy the connection string.
# If your password has @ : / ? # & =, URL-encode JUST the password before pasting.
# Example format:
# postgresql://USER:ENCODED_PASSWORD@HOST:5432/DATABASE?sslmode=require

DATABASE_URL="postgresql://<paste-your-neon-branch-prisma-url-here>"
PORT=4000